module.exports = (regex, string) => {
    let result = string.match(regex)
    if (result) {
        let num = 0;
        result.forEach(r => {
            num += Number(r.split(/[^0-9]/)[0]);
        });
        return num;
    } else {
        return 0;
    }
}