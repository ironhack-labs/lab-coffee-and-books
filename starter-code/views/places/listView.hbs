<section>
  {{#each places as |place|}}
  <article>
    <img src="{{place.image}}" alt="{{place.name}}">
    <h2><a href="/places/{{place._id}}">{{place.name}}</a></h2>
  </article>
  {{/each}}
</section>